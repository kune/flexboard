# Use flat node_modules (like npm) for maximum compatibility in Docker/CI.
# pnpm's default symlink-based approach can cause issues with CLI tools (vite, tsc)
# when running inside containers or across platforms.
shamefully-hoist=true
